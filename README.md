# Игра 4096


Классическая игра 2048, адаптированная на поле 5×5 с сохранением рекорда и контейнеризацией.

## 🎮 Особенности

- **Уникальное поле 5×5** — вместо стандартного 4×4
- **Полноценная игра 4096** — все базовые механики работают
- **Сохранение рекорда** — между игровыми сессиями в файл `highscore.txt`
- **Цветной интерфейс** — консольное оформление с подсветкой плиток
- **Кроссплатформенность** — работает на Windows, Linux, macOS
- **Контейнеризация** — готовый Dockerfile для развертывания

## 📋 Требования

- **Python 3.10+** (для локального запуска)
- **Docker** (для контейнеризации, опционально)
- **Git** (для клонирования репозитория)

## 🚀 Быстрый старт

### Способ 1: Через Python (рекомендуется)


# Клонируйте репозиторий
git clone https://github.com/zhek23-cmd/python-4096-game.git
cd python-4096-game

# Запустите игру
python main.py


### Способ 2: Через Docker


# Клонируйте репозиторий
git clone https://github.com/zhek23-cmd/python-4096-game.git
cd python-4096-game

# Сборка образа
docker build -t game-4096 .

# Запуск контейнера
docker run -it --rm game-4096


### Способ 3: Готовый EXE-файл (Windows)

1. Скачайте `Game4096.exe` из раздела [Релизы](ссылка)
2. Запустите двойным кликом

## 🎯 Управление

| Клавиша | Действие |
|---------|----------|
| **W / ↑** | Движение вверх |
| **A / ←** | Движение влево |
| **S / ↓** | Движение вниз |
| **D / →** | Движение вправо |
| **R** | Новая игра |
| **Q** | Выход из игры |

## 🎯 Цель игры

Объединяйте одинаковые плитки, чтобы получить плитку со значением **4096**!
- При объединении двух плиток одинакового значения получается плитка с удвоенным значением
- За каждое объединение начисляются очки
- Игра заканчивается, когда нет возможных ходов

## 📊 Система очков

- За каждое объединение вы получаете очки, равные значению новой плитки
- Пример: 2+2=4 → +4 очка, 4+4=8 → +8 очков
- Рекорд сохраняется в файл `highscore.txt` и загружается при следующем запуске

## 🐳 Docker

### Основные команды


# Сборка образа
docker build -t game-4096 .

# Запуск контейнера
docker run -it --rm game-4096

# Запуск с сохранением рекорда на хосте
docker run -it --rm -v "%cd%/highscore.txt:/app/highscore.txt" game-4096


### Проверка Dockerfile

Перед сборкой убедитесь, что Dockerfile корректен:


# Для Windows:
check_docker.bat


### Технические детали Docker

- **Базовый образ:** `python:3.10-slim`
- **Размер образа:** ~120-150 MB
- **Рабочая директория:** `/app`
- **Точка входа:** `python main.py`
- **Режим запуска:** интерактивный (`-it` флаг обязателен)

### ⚠️ Важное примечание

Для работы Docker требуется **включенная виртуализация в BIOS/UEFI**. Если Docker не запускается на вашем компьютере, используйте запуск через Python.

## 🛠️ Разработка

### Установка для разработки


# Клонирование репозитория
git clone https://github.com/zhek23-cmd/python-4096-game.git
cd python-4096-game

# Установка зависимостей для разработки
pip install -r requirements-dev.txt

# Установка pre-commit хуков
pre-commit install


### Проверка качества кода


# Запуск всех проверок
pre-commit run --all-files

# Форматирование кода
black .

# Сортировка импортов
isort .


### Тестирование


# Запуск игры в тестовом режиме
python main.py


### Сборка EXE-файла


# Установка PyInstaller
pip install pyinstaller

# Создание EXE-файла
pyinstaller --onefile --console --name Game4096 main.py

# Готовый файл: dist/Game4096.exe


## 📁 Структура проекта


python-4096-game/
├── main.py                    # Главный файл игры
├── board.py                   # Логика игрового поля (класс Board)
├── game.py                    # Управление игровым процессом (класс Game)
├── constants.py               # Константы и настройки
├── requirements.txt           # Зависимости (стандартная библиотека)
├── Dockerfile                 # Конфигурация Docker
├── .dockerignore              # Игнорируемые файлы для Docker
├── check_docker.bat           # Проверка Docker (Windows)
├── .gitignore                 # Игнорируемые файлы для Git
├── .pre-commit-config.yaml    # Настройка pre-commit
├── README.md                  # Эта документация
└── highscore.txt              # Файл с рекордом (создается автоматически)


## 📖 Документация по модулям

### main.py
Главный файл игры. Содержит:
- Игровой цикл
- Обработку пользовательского ввода
- Отображение интерфейса
- Управление состоянием игры

### board.py
Класс `Board` — отвечает за:
- Хранение состояния поля 5×5
- Логику движения плиток
- Объединение одинаковых плиток
- Проверку возможности ходов
- Добавление новых плиток

### game.py
Класс `Game` — управляет:
- Игровым процессом
- Сохранением и загрузкой рекорда
- Проверкой условий победы/поражения
- Обработкой ходов игрока

### constants.py
Содержит все константы:
- Размер поля (5×5)
- Вероятности появления плиток
- Управление (WASD)
- Цвета для консольного вывода

## 🐛 Известные ограничения

1. **Docker** — требует включенной виртуализации. На компьютерах без поддержки виртуализации не запускается.
2. **Консольный интерфейс** — игра работает только в терминалах, поддерживающих ANSI-цвета и UTF-8.
3. **Управление стрелками** — на некоторых системах может требовать дополнительной настройки терминала.

## 🔧 Устранение неполадок

### Проблема: Игра не запускается через Docker
**Решение:** Проверьте, включена ли виртуализация в BIOS/UEFI. Используйте запуск через Python.

### Проблема: Не работает управление стрелками
**Решение:** Используйте клавиши WASD. На Windows убедитесь, что запускаете через командную строку, а не PyScripter.